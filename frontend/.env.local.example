# 복사해서 .env.local 로 만든다. NEXT_PUBLIC_ 접두사 값은 브라우저에 노출되므로 공개 가능한 값만 넣는다.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key>
NEXT_PUBLIC_API_URL=http://localhost:8000
